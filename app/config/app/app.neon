################################################################################
#
# Application
#
# @package burza.grk.cz
# @author Milan Felix Sulc <sulcmil@gmail.com>
# @version $$REV$$
#
parameters:

    # System settings
    system:
        errorEmail: sulcmil@gmail.com
        errorPresenter: Front:Error

    # Site settings
    site:
        domain: burza.knihy.cz
        email: robot@burza.knihy.cz
        emailName: Burza GRK

    # Deploy settings
    deploy:
        rev: 20062015

    # Dirs
    # appDir, libsDir, tempDir, logDir -> already configured
    dirs:
        storage: %wwwDir%/uploads
        webtemp: %wwwDir%/temp

    # Paths
    paths:
        storage: uploads
        webtemp: temp

services:
    nette.userStorage:
        setup:
            - setNamespace(GFMP/Burza)